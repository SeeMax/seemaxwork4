$(document).ready(function() {
  $(function() {
    $(".rslides").responsiveSlides({
      auto: true,             // Boolean: Animate automatically, true or false
      speed: 10,            // Integer: Speed of the transition, in milliseconds
      timeout: 700,          // Integer: Time between slide transitions, in milliseconds
    });
  });
});
