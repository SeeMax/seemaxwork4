@import "variables.css.scss";

.work-page {
  color:$red;

  p {
    color:black;

    a {
      color:$red;
      border-bottom: 1px white solid;
      padding-bottom: 0px;
    }

    a:hover {
      border-bottom: 1px $red solid;
    }
  }

  .tower-screen {
    padding-top:60px;
  }
}

